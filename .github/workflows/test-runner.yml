name: TESTE RUNNER LOCAL

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Mostrar máquina
        run: hostname

      - name: Teste simples
        run: echo "RUNNER LOCAL FUNCIONANDO 🚀"
